@page "/RequestManagementTabs/RequestCreate/{senderId:guid}"
@rendermode InteractiveServer
@inject ISnackbar Snackbar
@inject NavigationManager NavigationManager
<CreateTeacherRequestComponent onAdded="Activate_01" SenderId="senderId"></CreateTeacherRequestComponent>

@code
{
    [Parameter]
    public Guid senderId { get; set; }

    private async Task Activate_01()
    {

        NavigationManager.NavigateTo($"https://localhost:7051/RequestManagementTabs");
        Snackbar.Add("Request Sent Successfully", Severity.Success);
    }
}
